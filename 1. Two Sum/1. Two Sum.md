1. Two Sum

Step1 ぱっと思いつくのは２重ループ

単純に計算する方法

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:

        for i in range(len(nums) - 1):
            for j in range(i + 1, len(nums)):
                if nums[i] + nums[j] == target:
                    return [i, j]
```



Step2


https://github.com/kitano-kazuki/leetcode/pull/11/changes/465bbb9bf73dcc7f48ab05ae12aeb2ad6518b2ce
辞書を使用することで空間と時間ともにONで解くことができる

https://github.com/ksaito0629/leetcode_arai60/pull/10/changes#diff-fb72ed84c22daaa065dd8aca8a790d2a4a20bd48c4db22d7605117f02be53c40R61
ソートするやり方もあるらしい

今回で自分のアルゴリズム能力の足りなさを思い知った
問題からそレを情報学的な要素に対応付けることと、対応付けた要素をどう使うかがぜんぜんだめ
どうすれば鍛えられるのか



```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:

        seen = {}

        for i, num in enumerate(nums):
            if target - num in seen:
                return [seen[target - num], i]
            seen[num] = i
        raise ValueError("not found")
```



