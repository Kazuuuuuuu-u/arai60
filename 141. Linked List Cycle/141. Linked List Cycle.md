```python
#数ヶ月か前にやったがもう一度
#Step1 何も見ずに
#最初から順番に辿っていって、nextですでに通ってきたノードが来たらそのノードの位置を返す
#すでに出てきたノードかどうかはsetでやればすぐに判定できそう
#Setに要素を追加するのってどうやってやるんだっけ
#https://docs.python.org/ja/3.13/library/stdtypes.html#set
#処理と変数宣言の部分を空行でわけてみた

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        visited_nodes=set()
        aNode=head

        while(aNode is not None):
            if aNode in visited_nodes:
                return True
            visited_nodes.add(aNode)
            aNode=aNode.next

        return False

#空間計算量O(1)のやり方
#Slowとfastみたいな変数名があったことだけ覚えていた
#テストケースで考えていたら、サイクルがあるときは早く進むほうが遅く進む方に追いつく事を思い出した

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        if not head:
            return False
        slow=head
        fast=head
        while fast.next and fast.next.next:
            slow=slow.next
            fast=fast.next.next
            if(slow==fast):
                return True

        return False



#Step2
#コードを整える
#他の人のレビューを見たらsetの内部構造まで気にしていた
#のでちょっとだけCPythonのsetobjectを見てみた
#殆どよくわからなかった。探索アルゴリズムはKnuth の Algorithm Dとやらをつかってるらしいてことぐ
#らい そもそもどう読めばがいいかがわかってない？
#Whileの条件式を()で囲う意味がない
#余計な空行を削除
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        visited=set()
        aNode=head

        while aNode is not None:
            if aNode in visited:
                return True
            visited.add(aNode)
            aNode=aNode.next
        
        return False

#https://github.com/appseed246/arai60/pull/2
#ここのコメントで、whileの条件式で使われているものが主役になってほしいと書かれていた
#今の状態だとfast.nextとfast.next.nextが主役になるべきだけど中ではfastに変わっている
#よって fastを使いたいのでfastをhead.nextで始める
#FastはListが与えられない場合にエラーが出そうなので最初で弾いて、1個の場合はNoneになる

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        if not head:
            return False
        
        slow=head
        fast=head.next

        while fast is not None and fast.next is not None:
            if slow==fast:
                return True
            slow=slow.next
            fast=fast.next.next
        
        return False

#動かない失敗コード
#Fastが一つしか進んでないので無限ループになるのはわかっていたが、fast.next.nextが大丈夫かどうか
#で悩んだ
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        if not head:
            return False
        
        slow=head
        fast=head.next

        while fast is not None and fast.next is not None:
            if slow==fast:
                return True
            slow=slow.next
            fast=fast.next
        
        return False


        
#Step3
#解法１
#1m20s
#56s
#1m5s

#解法２
#1m25s
#1m13s
#1m6s

```
