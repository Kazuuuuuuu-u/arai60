https://github.com/ryoooooory/LeetCode/pull/7/commits/226be592fa888113f6b25ebb9c8dfbc42da7137e
末尾は改行で終わる そういえばなんでだろう
https://saycon.co.jp/archives/neta/%E3%82%BD%E3%83%BC%E3%82%B9%E3%82%B3%E3%83%BC%E3%83%89%E6%9C%AB%E5%B0%BE%E3%81%AB%E6%94%B9%E8%A1%8C%E3%82%92%E5%85%A5%E3%82%8C%E3%82%8B%E3%81%B9%E3%81%8D%E7%90%86%E7%94%B1%E3%81%A8%E3%81%AF%EF%BC%9F
改行にするとdiffなどの差分もきれいに出るしそれまでのすべての行に改行があるのに最後だけしないのは一貫していない
改行があることを前提にしているコマンド等もあるし、改行のような行末マークがないときは動かないほうがいいプログラムもある
https://qiita.com/ko1nksm/items/3aea7fb8ac7e0396c10f

関数やメソッド、クラスにはdocstring書いたほうがいいらしい
https://peps.python.org/pep-0008/#other-recommendations
コメントとしてはもちろんだけど、docstringによるドキュメント自動生成されるときの内容はdocstringが対応するからかな？


Noneが保証されている場合はこちらで、そうじゃない場合はNoneじゃないものが入力によっては来るかもしれないから条件はピンポイントのほうがいいかなとは感じた

https://github.com/cheeseNA/leetcode/pull/6/files#diff-f0c9346ccaccbcb278b9a11d82e7dad004f4e6fa46308cc88f779c21725e014bR26

無限ループで処理することもできるけど、今回はnodeが移り変わっているのでそれを条件にするのが、よりループのたびに移り変わるイメージができていいのかなと思った
無限ループのほうが良い場合が実際にあったりするのか気になった
大域脱出という言葉を知らなかった
段階的な抜け出しではなくて、一気に任意の処理までもどれるてこと？
メリットとしては煩雑なエラーコードを書く必要がなくなること
デメリットとしては例えばメモリの解放とかが正常に行われないことがある
コードリーディングが難しくなるとかがある
ただ、言語それぞれで例外の機構はちがうだろうし結局使う言語の機構をちゃんと調べる意識が必要かも
 https://docs.google.com/document/d/1WPrRCrCJw5XqrvOgzo7Dlq06AniqpZoFq-pzTKkb1aw/edit?tab=t.0
https://qiita.com/Maki-Daisuke/items/80cbc26ca43cca3de4e4
https://ttsuki.github.io/styleguide/cppguide.ja.html#Exceptions
みたいなことを考えた


```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
	
        if not head:
            return False

        node = head
        visited = set()

        while node is not None:
            if node in visited:
                return True
            
            visited.add(node)
            node = node.next
        
        return False

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    “””
    Solution Class
    “””
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        “””
        find cycle
        “””
        if not head:
            return False

        slow = heads
        fast = head.next

        while fast is not None and fast.next is not None:
            if fast is slow:
                return True
            
            slow=slow.next
            fast=fast.next.next
        
        return False

```
