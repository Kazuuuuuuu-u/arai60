206. Reverse Linked List

Step1
最初に思いついたのは２つずつ見ていって、片方がNoneだったら終わり
1-2-3-4-5とあるとする。
1-2をみて、2のnextである3を記録しておく。その後に2のnextを１にして、1のnextをNoneにする
2-3をみて、3のnextを保持して矢印を2にする
これを繰り返してNoneになるまでやればできる？
最初だけ特殊だからフラグかなんかで管理しよう
もしくはループの前に処理すればいけるか

こんな考えなくてもいいかも
今のノードと同じノードを作って、それの頭につなげていけばいいだけか
時間計算量O(N)
空間も同じ
もっと自分のやることを言語化する必要がある。誤魔かしてるところがうまくできていない気がする。

最初に書いたもの
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if head is None or head.next is None:
            return head

        next_node = head.next
        reversed_head = ListNode(head.val, None)

        while next_node is not None:
            new_node = ListNode(next_node.val, reversed_head)
            reversed_head = new_node
            next_node = next_node.next
        return reversed_head
```

headを特別扱いしないでかけないかと考えて
書いたもの
ロジックがループの中で閉じられているから
こっちのほうがより見やすいと感じる
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if head is None or head.next is None:
            return head

        node = head
        reversed_head = None

        while node is not None:
            new_node = ListNode(node.val, reversed_head)
            reversed_head = new_node
            node = node.next
        return reversed_head
```

Step2
https://github.com/liruly/leetcode/pull/9#discussion_r2688471502
毎回再帰が選択肢にでてこない
KeitaOdaさんを参考
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        new_head, _ = self.reverse_recursive(node)
        return new_head

    def reverse_recursive(self, node):
        if node is None:
            return None, None
        if node.next is None:
            return node, node
        next_node = node.next
        node.next = None
        new_head, new_tail = self.reverse_recursive(next_node)
        new_tail.next = node
        new_tail = node
        return new_head, new_tail
```




